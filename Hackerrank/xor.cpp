#include<stdio.h>
#include<stdlib.h>
int main()
{
int len=(r-l)*(r-l);
	for(i=l;i<=len;i++)
{
	if(r1!=r)
	{
		ch=r1^l;
		if(ch>max)
		max=ch;
		l++;
	}
}
	printf("%d",10^10);
	system("pause");
	return 0;
}
