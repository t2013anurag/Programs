#include <stdio.h>
#include <stdlib.h>
int arr[1000000];
int main()
{
    int no,test,i;
    scanf("%d",&test);
    for(i=0;i<1000001;i++)
    arr[i]=0;
    while(test--)
    {
        scanf("%d",&no);
        arr[no]++;
    }
    for(i=1;i<1000001;i++)
    {
        while(arr[i]--){
            printf("%d\n",i);
 
        }
    }
    return 0;
}